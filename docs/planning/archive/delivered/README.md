# Delivered (optional)

Large **completed** backlog digests or historical “done” sections moved out of [`BACKLOG.md`](../../BACKLOG.md) to keep the live file scannable. Use the same YAML front matter and [`MANIFEST.md`](../MANIFEST.md) row as for `snapshots/`.
